import React, {Component} from "react";
import {Grid, Button, ButtonGroup, Typography} from "@material-ui/core";

export default class GymPage extends Component {
    constructor(props) {
        super(props);
    }

    render() {
        return (
            <div>
                NOICE. You rendered GymPage correctly !!
            </div>
        )
    }
}